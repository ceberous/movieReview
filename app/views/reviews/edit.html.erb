<h1 class="tcenter">Editing Review</h1>

<%= render 'form' %>

<div class="col-xs-10 col-xs-offset-1 col-sm-4 col-sm-offset-4 col-md-4 col-md-offset-4">
<% if @review.present? %>
	<%= button_to "Delete Review", movie_review_path(@movie,@review), data: { confirm: "Are you sure?" }, method: :delete , class: "btn btn-danger form-control" %>
<% end %>

</div>